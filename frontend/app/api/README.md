# API routes

HTTP handlers under `app/api/*/route.ts`. Used by the UI and external webhooks (Slack).

Examples: `/api/detect`, `/api/investigate`, `/api/incidents`, `/api/recover`.

Agents: [AGENTS.md](AGENTS.md).
