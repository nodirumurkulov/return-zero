# App routes

Next.js App Router: pages, layouts, and server actions.

| Area | Paths |
|------|--------|
| Catalog | `/catalog`, `/catalog/[productId]` |
| Incidents | `/incidents`, `/incidents/[incidentId]` |
| Auth | `/sign-in`, `/sign-up` |

API handlers live in [`api/`](api/). Agents: [AGENTS.md](AGENTS.md).
