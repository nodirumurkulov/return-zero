# Incident components

Incident command center UI: kanban columns, cards, action approval, timeline, agent findings.

Used on `/incidents` and `/incidents/[incidentId]`. Agents: [AGENTS.md](AGENTS.md).
