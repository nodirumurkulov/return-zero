# Frontend

Next.js app for Resolve. Uses Bun for install and CI.

```bash
bun install
bun run dev
```

Environment: copy repo `.env.example` to `.env.local` here.

Details for agents: [agents.md](agents.md).
