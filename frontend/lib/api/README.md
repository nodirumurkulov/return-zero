# API helpers

Small utilities for route handlers: JSON body parsing, Slack payload parsing.

Not domain business logic — use `lib/incidents`, `lib/detection`, etc. for that.

Agents: [AGENTS.md](AGENTS.md).
