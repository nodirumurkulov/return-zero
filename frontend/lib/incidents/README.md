# Incidents domain

Types, Supabase row mappers, and queries (`listIncidents`, `getIncidentDetail`).

Single source for the `Incident` type. Import: `@/lib/incidents`.

Agents: [AGENTS.md](AGENTS.md).
