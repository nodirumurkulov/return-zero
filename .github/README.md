# GitHub

CI runs on every pull request and push to `main`.

| Workflow | What it does |
|----------|----------------|
| [`workflows/ci.yml`](workflows/ci.yml) | Bun install, lint, typecheck, build in `frontend/` |

Agents: [AGENTS.md](AGENTS.md).
